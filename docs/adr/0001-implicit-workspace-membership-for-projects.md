# Implicit Workspace Membership for Projects

Projects do not maintain an independent membership roster or per-project access control list; all members of a Workspace have implicit access to all Projects within that Workspace, and task assignees are selected directly from Workspace members. We chose this model to eliminate redundant access sync and keep permission management lightweight for the MVP.

